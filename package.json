{
  "name": "photo-share",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "devDependencies": {
    "husky": "4.2.3",
    "lerna": "^3.20.2",
    "lint-staged": "10.0.8"
  },
  "scripts": {
    "lint": "lerna exec yarn lint",
    "start": "lerna exec yarn start",
    "start:dev": "lerna exec yarn start:dev"
  }
}
